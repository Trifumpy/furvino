import { type Config } from "prettier";

const config: Config = {
  trailingComma: "es5",
  arrowParens: "always",
  semi: true,
  singleQuote: true,
  useTabs: false,
  tabWidth: 2,
  printWidth: 80,
  bracketSpacing: true,
};

export default config;
